name = "ailex-api"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# Non-secret config can live here and be committed.
# Secrets (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY) must NOT go here —
# set them with `wrangler secret put <NAME>` instead. See README.md.

[observability]
enabled = true
