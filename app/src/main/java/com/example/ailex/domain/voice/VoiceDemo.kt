package com.example.ailex.domain.voice

/**
 * There is no real speech recognition in this build (see `CLAUDE.md`'s hard
 * constraints). This is the one fixed transcript `VoiceScreen` shows in its
 * Review stage — lifted verbatim from the prototype's voice demo state.
 */
object VoiceDemo {
    const val Transcript =
        "Someone called saying they were from my bank and forty thousand rupees has gone out of my account through UPI."
}
